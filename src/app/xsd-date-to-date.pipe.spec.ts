import { XsdDateToDatePipe } from './xsd-date-to-date.pipe';

describe('XsdDateToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new XsdDateToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
