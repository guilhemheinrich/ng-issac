import { PropertyAccessPipe } from './property-access.pipe';

describe('PropertyAccessPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyAccessPipe();
    expect(pipe).toBeTruthy();
  });
});
